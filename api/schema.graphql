
type Query {
  hello: String!
}

type Mutation {
  createUser (input: CreateUserInput!): User
}

type User {
  id: ID!
  name: String
}

input CreateUserInput {
  name: String!
  email: String!
}

type GithubCheck {
  id: ID!
}
