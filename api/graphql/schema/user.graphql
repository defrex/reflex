type Query {
  currentUser: User
}

type User {
  id: ID!
  name: String!
  figmaConnected: Boolean!
  githubConnected: Boolean!
}

type Mutation {
  logout: LogoutResponse
}

type LogoutResponse {
  status: MutationStatus
}
